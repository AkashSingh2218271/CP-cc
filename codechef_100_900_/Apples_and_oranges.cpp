#include <bits/stdc++.h>
using namespace std;

int main() {
    int x, a, b;
    cin >> x >> a >> b;
    
    if ((a + b) <= x) {
        cout << "yes\n";
    } else {
        cout << "no\n";    
    }
    
    return 0;
}

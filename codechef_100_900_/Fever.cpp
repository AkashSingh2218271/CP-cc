#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while (t--) {
        int temp;
        cin >> temp;
        
        if (temp > 98) {
            cout << "yes\n";
        } else {
            cout << "no\n";
        }
    }
    
    return 0;
}